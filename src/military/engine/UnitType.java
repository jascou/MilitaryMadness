package military.engine;

/**
 * Basic unit types. Existing code uses strings; this enum will be adopted gradually.
 */
public enum UnitType {
    INFANTRY,
    TANK,
    ARTILLERY,
    AIR,
    UNKNOWN
}
